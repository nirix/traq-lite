<?php $this->extend('layouts/admin.phtml'); ?>

<?php $this->startSection('content'); ?>
<div class="projects container-fluid">
    <div class="pull-right">
        <a href="<?=baseUrl('/admin/projects/new')?>" class="btn btn-sm btn-success">
            <span class="fa fa-plus"></span> <?=t('new_project')?>
        </a>
    </div>

    <h1 class="page-header"><?=t('projects')?></h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th><?=t('name')?></th>
                <th><?=t('slug')?></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($projects as $project) : ?>
            <tr>
                <td><a href="<?=baseUrl("/admin/projects/{$project['id']}/edit")?>"><?=$project['name']?></a></td>
                <td><?=$project['slug']?></td>
                <td>
                    <div class="text-right">
                        <a href="<?=baseUrl("/admin/projects/{$project['id']}/edit")?>" class="btn btn-xs btn-warning" title="<?=t('edit')?>">
                            <span class="fa fa-pencil"></span> <span class="sr-only"><?=t('edit')?></span>
                        </a>

                        <a href="<?=baseUrl("/admin/projects/{$project['id']}/delete")?>" class="btn btn-xs btn-danger" title="<?=t('delete')?>" data-confirm="<?=t('confirm.delete_x', [$project['name']])?>">
                            <span class="fa fa-trash"></span> <span class="sr-only"><?=t('delete')?></span>
                        </a>
                    </div>
                </td>
            </tr>
        <?php endforeach ?>
        </tbody>
    </table>
</div>
<?php $this->endSection(); ?>
